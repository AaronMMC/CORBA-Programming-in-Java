module ModifiedHangman {
    exception LogInException {};

    interface AuthenticationService {
        // returns a token string
        string login(in string username, in string password) raises(LoginException);
        void logout(in string token) raises (PlayerNotLoggedInException);
    };

};